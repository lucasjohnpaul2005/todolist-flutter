# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
pubspec.lock

# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store

# Generated code (freezed / auto_route) — regenerate locally with:
# dart run build_runner build --delete-conflicting-outputs
# Not gitignored by default in the mentor's template (it commits them),
# so remove these two lines if you want to commit generated output the
# same way that template does.

